[project]
name = "libs-selection"
version = "0.1.0"
description = "Scrape the top-100 Java libraries from Maven Central."
requires-python = ">=3.13"
dependencies = [
    "beautifulsoup4>=4.12",
    "selenium>=4.20",
]

[tool.uv]
package = false
