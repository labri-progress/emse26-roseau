[project]
name = "exclusion-conventions"
version = "0.1.0"
description = "Inventory of annotation and package-name exclusions in the longitudinal corpus."
requires-python = ">=3.13"
dependencies = [
    "matplotlib==3.10.1",
    "pandas==2.2.3",
    "pyyaml>=6.0",
]

[tool.uv]
package = false
