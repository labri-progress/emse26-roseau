# Preliminary longitudinal analysis

`analysis.ipynb` summarises an early pilot run. It expects `preliminary_results.csv`
(pipe-separated, not shipped in this package); without it the notebook stops at the
first cell. The final longitudinal study is in [../walk/notebooks](../walk/notebooks/).

Environment is managed from the repo root (`uv sync` once). Then, from here: `uv run jupyter lab`.
