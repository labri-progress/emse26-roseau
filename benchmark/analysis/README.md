# RQ1 accuracy analysis

`preliminary-analysis.ipynb` computes the accuracy metrics and plots from the raw
tool results in [data/](data/).

Environment is managed from the repo root (`uv sync` once). Then, from here:

```bash
uv run jupyter lab
```
