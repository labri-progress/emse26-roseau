[project]
name = "build-times"
version = "0.1.0"
description = "RQ3 preliminary study: per-commit JAR build times for the longitudinal corpus."
requires-python = ">=3.13"
dependencies = [
    "pyyaml>=6.0",
]

[tool.uv]
package = false
