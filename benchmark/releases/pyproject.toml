[project]
name = "release-study"
version = "0.1.0"
description = "RQ3 follow-up: commit-level breaking changes vs. breaking changes between released JARs."
requires-python = ">=3.13"
dependencies = [
    "pyyaml>=6.0",
]

[tool.uv]
package = false
