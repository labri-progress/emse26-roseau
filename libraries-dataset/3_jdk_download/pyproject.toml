[project]
name = "jdk-download"
version = "0.1.0"
description = "Download and prepare the JDK sources used by the benchmark."
requires-python = ">=3.13"
dependencies = [
    "gitpython>=3.1",
    "setuptools>=75",
]

[tool.uv]
package = false
